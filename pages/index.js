import GlobalStyles, {Button} from "../utils/globalStyles";

const index = () => {
  return (
    <main>
      <GlobalStyles/>
      <Button >Play now</Button>
    </main>
  )
};

export default index;